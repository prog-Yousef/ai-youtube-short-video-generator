/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com'], // Add the domain for the images other wise it will not work error will be shown
    },
};

export default nextConfig;
